% QSM script
% B0 = 3;
% H = [0,0,1];

% load multi-echo data [nx,ny,nz,ncoil]
% I =

% Mask for soft tissue
% tmask = 
% dTE =, TE0=, or define TE array

% raw_phase = 
% [Tissue_Phase, Tissue_Mask, bkg_phase] = VSHARP(raw_phase,tmask,12);
% QSM = QSM_solver(Tissue_Phase=, Tissue_Mask, dTE, B0);